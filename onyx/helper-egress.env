# Stack-owned direct destinations for Onyx backend HTTP clients.
# Keep this list aligned with the service names and host aliases in the Compose
# model. Public destinations must go through onyx-public-egress-bridge.
NO_PROXY=127.0.0.1,localhost,::1,api_server,background,web_server,nginx,code-interpreter,relational_db,cache,opensearch,index,minio,teep,local-embedding-shim,doc-drop-web,searxng-service-gateway,obscura-cdp-gateway
no_proxy=127.0.0.1,localhost,::1,api_server,background,web_server,nginx,code-interpreter,relational_db,cache,opensearch,index,minio,teep,local-embedding-shim,doc-drop-web,searxng-service-gateway,obscura-cdp-gateway
